const products_middlewares = require("./products.middlewares");
const sales_middlewares = require("./sales.middlewares");
const users_middlewares = require("./users.middlewares");

module.exports = Object.freeze({
    products_middlewares,
    sales_middlewares,
    users_middlewares

})